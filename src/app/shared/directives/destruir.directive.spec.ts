import { DestruirDirective } from './destruir.directive';

describe('DestruirDirective', () => {
  it('should create an instance', () => {
    const directive = new DestruirDirective();
    expect(directive).toBeTruthy();
  });
});
